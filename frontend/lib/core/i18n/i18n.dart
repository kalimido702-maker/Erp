/// i18n barrel — import this one file to get the whole localization toolkit:
/// `AppLocale`, `LocalizedValue`, the controller/providers, and the
/// `context.tr()` / `context.localized()` extensions.
library;

export 'app_locale.dart';
export 'i18n_controller.dart';
export 'localized_value.dart';
export 'translation_repository.dart';
export 'translations_scope.dart';
